version: "3.9"

services:
  # ─── PostgreSQL ───────────────────────────────────────────
  postgres:
    build:
      context: .
      dockerfile: Dockerfile.postgres
    container_name: verifa_postgres
    restart: unless-stopped
    mem_limit: 512m
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-verifa}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-verifa}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    command: >
      postgres
      -c shared_preload_libraries=pgaudit
      -c pgaudit.log=write,ddl
      -c pgaudit.log_catalog=off
      -c pgaudit.log_relation=on
      -c log_line_prefix="%m [%p] %u@%d "
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U verifa"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ─── Next.js Frontend + API ───────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: verifa-frontend
    container_name: verifa_frontend
    restart: unless-stopped
    mem_limit: 512m
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-verifa}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-verifa}
      DATABASE_URL_UNPOOLED: postgresql://${POSTGRES_USER:-verifa}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-verifa}
      NEXTAUTH_URL: ${NEXTAUTH_URL:-http://localhost:3000}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET:-change_me_in_production_please}
      WORKER_URL: http://worker:8000
      WORKER_SECRET: ${WORKER_SECRET:-change_me_worker_secret}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      AZURE_AD_CLIENT_ID: ${AZURE_AD_CLIENT_ID:-}
      AZURE_AD_CLIENT_SECRET: ${AZURE_AD_CLIENT_SECRET:-}
      AZURE_AD_TENANT_ID: ${AZURE_AD_TENANT_ID:-}
      CRON_SECRET: ${CRON_SECRET:-}
      S3_BUCKET: ${S3_BUCKET:-}
      S3_REGION: ${S3_REGION:-auto}
      S3_ENDPOINT: ${S3_ENDPOINT:-}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
      RESULTS_DIR: /app/results
      NODE_ENV: production
    ports:
      - "3000:3000"
    volumes:
      - ./worker/results:/app/results:ro  # read-only prístup k vygenerovaným PDF

  # ─── Redis (Arq Broker) ───────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: verifa_redis
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD:-verifa_redis_dev}
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-verifa_redis_dev}", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ─── Browserless (Playwright Scraping) ────────────────────
  browserless:
    image: ghcr.io/browserless/chromium:latest
    container_name: verifa_browserless
    restart: unless-stopped
    shm_size: 1g
    mem_limit: 2g
    environment:
      - CONNECTION_TIMEOUT=300000
      - MAX_CONCURRENT_SESSIONS=8
      - TOKEN=${BROWSERLESS_TOKEN:-}

  # ─── Python FastAPI Worker ────────────────────────────────
  worker:
    build:
      context: ./worker
      dockerfile: Dockerfile
    image: verifa-worker-image
    container_name: verifa_worker
    restart: unless-stopped
    mem_limit: 1g
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-verifa}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-verifa}
      APP_ENV: development
      RESULTS_DIR: /app/results
      TZ: Europe/Bratislava
      PLAYWRIGHT_HEADLESS: "false"
      PLAYWRIGHT_TIMEOUT: "60000"
      WORKER_SECRET: ${WORKER_SECRET:-change_me_worker_secret}
      REDIS_URL: redis://:${REDIS_PASSWORD:-verifa_redis_dev}@redis:6379/0
      CRE_USERNAME: ${CRE_USERNAME:-}
      CRE_PASSWORD: ${CRE_PASSWORD:-}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      S3_BUCKET: ${S3_BUCKET:-}
      S3_REGION: ${S3_REGION:-auto}
      S3_ENDPOINT: ${S3_ENDPOINT:-}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
      EXPERT_MODE: "1"
    volumes:
      - ./worker/results:/app/results  # Evidence Binder PDF súbory

  # ─── Arq Background Worker ────────────────────────────────
  arq_worker:
    build:
      context: ./worker
      dockerfile: Dockerfile
    image: verifa-worker-image
    container_name: verifa_arq_worker
    restart: unless-stopped
    mem_limit: 2g
    command: arq src.worker_arq.WorkerSettings
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-verifa}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-verifa}
      APP_ENV: development
      RESULTS_DIR: /app/results
      TZ: Europe/Bratislava
      PLAYWRIGHT_HEADLESS: "false"
      PLAYWRIGHT_TIMEOUT: "60000"
      WORKER_SECRET: ${WORKER_SECRET:-change_me_worker_secret}
      REDIS_URL: redis://:${REDIS_PASSWORD:-verifa_redis_dev}@redis:6379/0
      SENTRY_DSN: ${SENTRY_DSN:-}
      CRE_USERNAME: ${CRE_USERNAME:-}
      CRE_PASSWORD: ${CRE_PASSWORD:-}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      S3_BUCKET: ${S3_BUCKET:-}
      S3_REGION: ${S3_REGION:-auto}
      S3_ENDPOINT: ${S3_ENDPOINT:-}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
      EXPERT_MODE: "1"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8011/health')"]
      interval: 10s
      timeout: 5s
      retries: 3
    volumes:
      - ./worker/results:/app/results

  # ─── DB Backup (cron pg_dump at 3am) ──────────────────────
  db-backup:
    image: postgres:16-alpine
    container_name: verifa_db_backup
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_USER: verifa
      DB_NAME: verifa
      BACKUP_DIR: /backups
      RETENTION_DAYS: "30"
      S3_BUCKET: ${S3_BACKUP_BUCKET:-}
      PGPASSWORD: ${POSTGRES_PASSWORD}
      TZ: Europe/Bratislava
    volumes:
      - db_backups:/backups
      - ./scripts/backup-db.sh:/backup-db.sh:ro
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        echo "0 3 * * * /backup-db.sh >> /var/log/backup.log 2>&1" > /etc/crontabs/root &&
        crond -f -l 2

volumes:
  postgres_data:
  db_backups:
